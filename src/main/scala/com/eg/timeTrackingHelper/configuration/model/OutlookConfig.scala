package com.eg.timeTrackingHelper.configuration.model

import java.net.URI

case class OutlookConfig(
                          email: String,
                          password: String,
                          url: URI
                        )
